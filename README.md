# Business Management System — демо-материалы

Выдержки из закрытого коммерческого проекта: система учёта для малого бизнеса с доставкой (заказы, склад, финансы, CRM, логистика, Telegram-бот). Исходный код закрыт, поэтому здесь только то, что можно показать: фрагмент кода, прогон тестов, структура миграций и список эндпоинтов API. Данные клиентов, названия товаров и суммы в материалах отсутствуют или замаскированы. Внутреннее имя проекта: nerest_family. История: 968 коммитов, октябрь 2025 — сентябрь 2026, один автор (два имени в shortlog — одна учётная запись, сервер и ноутбук).

## Стек

Python 3.12 · FastAPI · SQLAlchemy 2 (async) · Alembic · PostgreSQL · Redis · aiogram 3 · Telethon · Next.js / React / Tailwind

## Что внутри

| Файл | Что показывает |
|------|----------------|
| `code/stock_fefo_excerpt.py` | Фрагмент складского сервиса: списание партий по FEFO (ближайший срок годности первым) с `SELECT ... FOR UPDATE` против гонки параллельных списаний, журнал по каждой партии |
| `code/telegram_sync_excerpt.py` | Фрагмент сервиса синхронизации Telegram-переписки в CRM: нейтральный dataclass сообщения и идемпотентная дедупликация |
| `screenshots/pytest.png` | Прогон юнит-тестов синхронизации Telegram и импорта WhatsApp: 65 passed |
| `migrations.txt` | Имена 87 миграций Alembic (сейчас 81 файл в versions/ после merge-ревизий) — история развития схемы БД |
| `screenshots/api-docs.png` | Swagger /docs: первые разделы API |
| `screenshots/api-docs-groups.png` | Swagger /docs: все группы эндпоинтов (233 эндпоинта) |

## Архитектурная идея фрагмента

Синхронизация разделена на два слоя. Слой чистой логики не импортирует telethon и работает только с `IncomingTgMessage`, поэтому покрыт юнит-тестами на in-memory БД. Тонкая обёртка над telethon переводит его объекты в этот dataclass. Дедупликация по паре `(customer_id, telegram_message_id)` делает повторную синхронизацию истории безопасной.
